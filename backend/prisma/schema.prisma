datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// TODO: Add models here as project logic is defined
// model Document {
//   id        String   @id @default(uuid())
//   name      String
//   path      String
//   createdAt DateTime @default(now())
// }
